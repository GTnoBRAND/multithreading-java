package org.example.executorService.concurrentMap;


/**
 * The ConcurrentNavigableMap in Java is
 * a thread-safe sub-interface of ConcurrentMap and
 * NavigableMap. It provides concurrent access, atomic updates,
 * and sorted key navigation, allowing safe sub-map operations
 * (subMap, headMap, tailMap) across multiple threads without external locks.
 * Its primary implementation is ConcurrentSkipListMap.*/
public class ConcurrentNavigableMap {
    static void main() {

    }
}
